const {Itinerary} = require('../src/itinerary.js')

describe('tests', () => {
    let itinerary;
    beforeEach(() => {
        itinerary = new Itinerary('South Mediterranean')
    })
describe('Itinerary', () => {
    it('can be instantiated', () => {
        expect(itinerary).toBeInstanceOf(Object);
    })
    it('has a ports property', () => {
     })
   })
})